# dependencies
/node_modules
/bot-service/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local
# never commit .env backups — they have leaked real secrets before
.env.bak*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# drizzle — migrations are version-controlled (the `migrate` compose service
# applies them on deploy). Ignore only drizzle-kit's local studio/meta cruft.
/drizzle/.drizzle

# audio storage
/audio-storage
.env.docker

# Python bytecode (diarization-service)
__pycache__/
*.pyc
